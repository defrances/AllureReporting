<div class="{{b 'pane' 'section'}}">
    <div class="{{b cls 'container'}}">
        <h3 class="{{b cls 'title'}}">{{t 'testResult.pdf.title'}}</h3>
        <p class="{{b cls 'description'}}">{{t 'testResult.pdf.description'}}</p>
        <button class="pdf-download-button {{b cls 'button'}}" type="button">
            {{allure-icon 'pdf'}}
            <span>{{t 'testResult.pdf.download'}}</span>
        </button>
    </div>
</div>

